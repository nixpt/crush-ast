[package]
name = "crush-errors"
version = "0.2.0"
edition = "2024"
description = "Unified error types for the CRUSH runtime"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror.workspace = true
regex = "1.0"
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
