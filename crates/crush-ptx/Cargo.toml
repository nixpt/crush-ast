[package]
name = "crush-ptx"
version = "0.1.0"
edition = "2021"

[dependencies]
casm = { path = "../casm", version = "0.3.0" }
crush-errors = { path = "../crush-errors", version = "0.3.0" }
serde_json = "1.0"

[dev-dependencies]
crush-frontend = { path = "../crush-frontend" }
