// Generated by ts-rs from crush-cast Rust types.
// Do not edit manually — re-run `cargo run -p crush-cast --bin export-ts --features ts-export`.

export type JsonValue = number | string | boolean | Array<JsonValue> | { [key in string]: JsonValue } | null;

export type Program = { cast_version: string, entry: string, lang: string | null, functions: { [key in string]: Function }, 
/**
 * AI execution metadata (goals, tool-chains, delegation).
 */
ai_meta?: AIMetadata | null, 
/**
 * Navigation manifest (@module annotation): purpose, exports, invariants,
 * related modules. Consumed by crush-index to build the queryable index.
 */
manifest?: ModuleManifest | null, 
/**
 * Exhaustive match sites for tracked sum types (compiler-populated).
 * Set for each type listed in `manifest.exhaustive_types`.
 */
exhaustive_sites?: Array<ExhaustiveMatchSite>, 
/**
 * Work-in-progress node, if a `@wip { ... }` block was declared.
 */
wip?: WipNode | null, 
/**
 * Technical-debt nodes from `@temporary { ... }` blocks.
 */
temporaries?: Array<TemporaryNode>, 
/**
 * Architectural decision records from `@decision "name" { ... }` blocks.
 */
decisions?: Array<DecisionNode>, };

export type Function = { params: Array<[string, CastType]>, body: Array<Statement>, meta: { [key in string]: JsonValue }, 
/**
 * Whether this is an async function (marked with `async` keyword).
 * Used for tooling and frontend lowering; spawn/await behavior is
 * explicit via the `spawn` expression and `AWAIT` opcode.
 */
is_async?: boolean, 
/**
 * Semantic annotations (@errors, @reads, @writes, @covers, @relies-on).
 * Absent when no annotations were written; all sub-fields are optional.
 */
annotations?: FunctionAnnotations | null, };

/**
 * Language-agnostic type system for CAST
 */
export type CastType = "Int" | "Float" | "String" | "Bool" | "Null" | { "Array": CastType } | { "Map": CastType } | { "Struct": string } | { "Lambda": { params: Array<CastType>, returns: CastType, } } | "Any" | { "TypeRef": string };

export type Statement = { "type": "VarDecl", name: string, value: Expression, type_hint: CastType, meta: { [key in string]: JsonValue }, } | { "type": "Export", name: string, value: Expression, meta: { [key in string]: JsonValue }, } | { "type": "ExprStmt", expr: Expression, meta: { [key in string]: JsonValue }, } | { "type": "If", condition: Expression, then_body: Array<Statement>, else_body: Array<Statement> | null, meta: { [key in string]: JsonValue }, } | { "type": "While", condition: Expression, body: Array<Statement>, meta: { [key in string]: JsonValue }, } | { "type": "For", variable: string, iterable: Expression, body: Array<Statement>, meta: { [key in string]: JsonValue }, } | { "type": "Return", value: Expression | null, meta: { [key in string]: JsonValue }, } | { "type": "TryCatch", body: Array<Statement>, error_var: string, handler: Array<Statement>, meta: { [key in string]: JsonValue }, } | { "type": "Throw", value: Expression, meta: { [key in string]: JsonValue }, } | { "type": "FunctionDef", name: string, params: Array<[string, CastType]>, body: Array<Statement>, meta: { [key in string]: JsonValue }, } | { "type": "SetField", target: Expression, field: string, value: Expression, meta: { [key in string]: JsonValue }, } | { "type": "LangBlock", 
/**
 * Language name: python, javascript, rust, etc.
 */
lang: string, 
/**
 * Raw source code to execute
 */
code: string, 
/**
 * Variables to inject into the sandbox
 */
variables: Array<string>, 
/**
 * Import statements within the block
 */
imports: Array<ImportStatement>, meta: { [key in string]: JsonValue }, } | { "type": "Import", 
/**
 * The import to resolve
 */
import: ImportStatement, meta: { [key in string]: JsonValue }, } | { "type": "StructDef", name: string, fields: Array<[string, CastType]>, meta: { [key in string]: JsonValue }, } | { "type": "Break", meta: { [key in string]: JsonValue }, } | { "type": "Continue", meta: { [key in string]: JsonValue }, } | { "type": "DomMutate", target: Expression, mutation_type: DomMutationType, value: Expression | null, value2: Expression | null, meta: { [key in string]: JsonValue }, } | { "type": "DomEventListener", target: Expression, event: string, callback: Expression, meta: { [key in string]: JsonValue }, } | { "type": "AI" } & AIStatement;

export type Expression = { "type": "IntLiteral", value: bigint, meta: { [key in string]: JsonValue }, } | { "type": "FloatLiteral", value: number, meta: { [key in string]: JsonValue }, } | { "type": "StringLiteral", value: string, meta: { [key in string]: JsonValue }, } | { "type": "BoolLiteral", value: boolean, meta: { [key in string]: JsonValue }, } | { "type": "NullLiteral", meta: { [key in string]: JsonValue }, } | { "type": "Var", name: string, meta: { [key in string]: JsonValue }, } | { "type": "BinaryOp", operator: string, left: Expression, right: Expression, meta: { [key in string]: JsonValue }, } | { "type": "UnaryOp", operator: string, operand: Expression, meta: { [key in string]: JsonValue }, } | { "type": "Call", function: string, args: Array<Expression>, meta: { [key in string]: JsonValue }, } | { "type": "CapabilityCall", name: string, args: Array<Expression>, meta: { [key in string]: JsonValue }, } | { "type": "Pipeline", segments: Array<Expression>, meta: { [key in string]: JsonValue }, } | { "type": "Spawn", function: string, args: Array<Expression>, meta: { [key in string]: JsonValue }, } | { "type": "Lambda", params: Array<[string, CastType]>, body: Array<Statement>, meta: { [key in string]: JsonValue }, } | { "type": "Yield", meta: { [key in string]: JsonValue }, } | { "type": "NewStruct", name: string, meta: { [key in string]: JsonValue }, } | { "type": "GetField", target: Expression, field: string, meta: { [key in string]: JsonValue }, } | { "type": "Range", start: Expression, end: Expression, meta: { [key in string]: JsonValue }, } | { "type": "Await", expression: Expression, meta: { [key in string]: JsonValue }, } | { "type": "ArrayLiteral", elements: Array<Expression>, meta: { [key in string]: JsonValue }, } | { "type": "ObjectLiteral", properties: Array<[string, Expression]>, meta: { [key in string]: JsonValue }, } | { "type": "Index", target: Expression, index: Expression, meta: { [key in string]: JsonValue }, } | { "type": "DomQuery", query_type: DomQueryType, selector: Expression, meta: { [key in string]: JsonValue }, } | { "type": "Match", expression: Expression, arms: Array<MatchArm>, meta: { [key in string]: JsonValue }, } | { "type": "AI" } & AIExpression;

export type DomMutationType = { "type": "SetTextContent" } | { "type": "SetAttribute" } | { "type": "RemoveAttribute" } | { "type": "SetStyle" } | { "type": "SetInnerHtml" } | { "type": "AppendHtml" } | { "type": "Remove" } | { "type": "AddClass" } | { "type": "RemoveClass" };

export type DomQueryType = { "type": "QuerySelector" } | { "type": "QuerySelectorAll" } | { "type": "GetElementById" } | { "type": "GetElementsByClassName" } | { "type": "GetElementsByTagName" };

export type MatchArm = { pattern: Pattern, body: Array<Statement>, };

export type Pattern = { "type": "Literal", value: Expression, } | { "type": "Identifier", name: string, } | { "type": "Struct", name: string, fields: Array<[string, Pattern]>, } | { "type": "Wildcard" };

export type ImportStatement = { "type": "CrushModule", module_path: string, alias: string | null, selective: Array<string>, } | { "type": "PolyglotModule", language: string, module_path: string, alias: string | null, selective: Array<string>, } | { "type": "MCPImport", server_url: string, tools: Array<string>, alias: string | null, } | { "type": "Capability", capability_path: string, permissions: Array<string>, alias: string | null, } | { "type": "External", uri: string, resource_type: ExternalResourceType, alias: string | null, } | { "type": "SecureEnv", 
/**
 * Specific keys to import (empty = all keys)
 */
keys: Array<string>, 
/**
 * Alias for the imported secrets module
 */
alias: string | null, 
/**
 * Path to the secrets database (optional, uses default if not specified)
 */
db_path: string | null, };

export type ExternalResourceType = "Http" | "Git" | "File" | "Database" | { "API": { format: string, } };

/**
 * Extended AST with AI-native constructs
 */
export type AIExpression = { "ai_type": "Query", 
/**
 * Natural language query for AI processing
 */
query: string, 
/**
 * Expected result type
 */
result_type: string | null, 
/**
 * Context information
 */
context: { [key in string]: JsonValue }, } | { "ai_type": "ToolChain", 
/**
 * Chain of tool calls
 */
tools: Array<ToolCall>, 
/**
 * Execution strategy (sequential, parallel, conditional)
 */
strategy: ExecutionStrategy, 
/**
 * Error handling strategy
 */
error_handling: ErrorHandling, } | { "ai_type": "AgentDelegation", 
/**
 * Task description for delegation
 */
task: string, 
/**
 * Target agents (can be patterns or specific IDs)
 */
agents: Array<string>, 
/**
 * Delegation strategy
 */
delegation_strategy: DelegationStrategy, 
/**
 * Expected result format
 */
expected_format: string | null, } | { "ai_type": "LearningLoop", 
/**
 * What to learn from
 */
learning_target: LearningTarget, 
/**
 * Learning strategy
 */
strategy: LearningStrategy, 
/**
 * Adaptation actions
 */
adaptations: Array<AdaptationAction>, } | { "ai_type": "ContextAware", 
/**
 * The wrapped expression
 */
expression: Expression, 
/**
 * Context requirements
 */
requires_context: Array<string>, 
/**
 * Context providers
 */
provides_context: Array<string>, } | { "ai_type": "SemanticMatch", 
/**
 * The expression to evaluate
 */
target: Expression, 
/**
 * The natural language concept to match against
 */
concept: string, 
/**
 * Confidence threshold for the match (e.g., 0.85)
 */
confidence_threshold: number, } | { "ai_type": "Synthesize", 
/**
 * The target type to generate
 */
output_type: CastType, 
/**
 * Constraints or instructions for generation
 */
constraints: Array<string>, 
/**
 * Values or variables to feed into the prompt context
 */
context_refs: Array<Expression>, 
/**
 * Optional few-shot examples
 */
examples: Array<Expression> | null, };

/**
 * AI-Native Statement extensions
 */
export type AIStatement = { "ai_type": "GoalDeclaration", goal_id: string, description: string, success_criteria: Array<string>, priority: Priority, deadline: string | null, } | { "ai_type": "ProgressUpdate", goal_id: string, progress: number, status_message: string, metrics: { [key in string]: number }, } | { "ai_type": "KnowledgeSharing", knowledge_type: KnowledgeType, content: JsonValue, recipients: Array<string>, retention_policy: RetentionPolicy, } | { "ai_type": "CapabilityDiscovery", domain: string, requirements: Array<string>, discovery_strategy: DiscoveryStrategy, } | { "ai_type": "AdaptationRequest", adaptation_type: AdaptationType, reason: string, parameters: { [key in string]: JsonValue }, } | { "ai_type": "SemanticSwitch", 
/**
 * The expression to evaluate
 */
target: Expression, 
/**
 * Cases map a natural language concept to a body of statements
 */
cases: Array<[string, Array<Statement>]>, 
/**
 * Optional fallback block if no concept matches well enough
 */
fallback: Array<Statement> | null, };

export type AIMetadata = { description: string, ai_tags: Array<string>, required_capabilities: Array<string>, execution_context: ExecutionContext, learning_objectives: Array<string>, collaboration_patterns: Array<CollaborationPattern>, inputs: Array<ParameterSchema>, outputs: Array<ParameterSchema>, complexity: number, };

/**
 * Tool call specification
 */
export type ToolCall = { tool_name: string, parameters: { [key in string]: JsonValue }, result_binding: string | null, condition: string | null, required_capability: string | null, };

/**
 * Execution strategies for tool chains
 */
export type ExecutionStrategy = { "type": "Sequential" } | { "type": "Parallel" } | { "type": "Conditional", conditions: Array<string>, } | { "type": "Retry", max_attempts: number, backoff_strategy: BackoffStrategy, };

/**
 * Error handling strategies
 */
export type ErrorHandling = { "type": "FailFast" } | { "type": "ContinueOnError" } | { "type": "Retry", max_retries: number, retry_condition: string | null, } | { "type": "Fallback", fallback_tools: Array<ToolCall>, };

/**
 * Backoff strategies for retries
 */
export type BackoffStrategy = { "type": "Fixed", delay_ms: bigint, } | { "type": "Exponential", base_delay_ms: bigint, max_delay_ms: bigint, } | { "type": "Linear", increment_ms: bigint, };

/**
 * Delegation strategies for agent coordination
 */
export type DelegationStrategy = "FirstAvailable" | "CapabilityMatch" | "ParallelSplit" | "Hierarchical" | { "Consensus": { threshold: number, } } | "Broadcast" | "Best" | "RoundRobin";

/**
 * Learning targets
 */
export type LearningTarget = "UserBehavior" | "ExecutionPatterns" | "ErrorPatterns" | "PerformanceMetrics" | "ToolUsage";

/**
 * Learning strategies
 */
export type LearningStrategy = "PatternRecognition" | "StatisticalAnalysis" | "MachineLearning" | "RuleBased";

/**
 * Adaptation actions
 */
export type AdaptationAction = "OptimizeToolChain" | "ImproveErrorHandling" | "UpdateAgentSelection" | "ModifyExecutionStrategy" | "LearnNewPatterns";

/**
 * Priority levels for goals and tasks
 */
export type Priority = "Low" | "Medium" | "High" | "Critical";

/**
 * Types of knowledge for sharing
 */
export type KnowledgeType = "Pattern" | "Solution" | "BestPractice" | "Warning" | "Insight";

/**
 * Knowledge retention policies
 */
export type RetentionPolicy = "Ephemeral" | "Session" | "Persistent" | { "Conditional": { condition: string, } };

/**
 * Discovery strategies for capabilities
 */
export type DiscoveryStrategy = "Broadcast" | "Targeted" | "Hierarchical" | "LearningBased";

/**
 * Types of adaptation requests
 */
export type AdaptationType = "Performance" | "Reliability" | "Usability" | "Compatibility" | "Learning";

export type ParameterSchema = { name: string, description: string, type_hint: string, required: boolean, default_value: JsonValue | null, };

export type ToolSchema = { name: string, description: string, parameters: { [key in string]: ParameterSchema }, return_type: string, mcp_server: string | null, mcp_method: string | null, };

export type LearningSource = "ExecutionResults" | "UserFeedback" | "EnvironmentObservations" | { "PeerAgents": { agent_ids: Array<string>, } };

export type AdaptationStrategy = "PerformanceOptimization" | "Personalization" | "CapabilityExpansion" | "CollaborationEnhancement";

export type ExecutionContext = { environment: Array<string>, resources: Array<string>, permissions: Array<string>, dependencies: Array<string>, };

export type CollaborationPattern = { pattern_type: string, participants: Array<string>, communication_style: string, decision_making: string, };

