[package]
license = "MIT OR Apache-2.0"
name = "bash_walker"
version = "0.1.0"
edition = "2021"

[dependencies]
casm = { path = "../casm" }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
shlex = "1.3"
