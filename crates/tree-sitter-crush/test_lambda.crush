// expect-error: [runtime] Parse errors: Unexpected token at line 1, column 13: unexpected token in expression: `,...
let add = |a, b| {
    return a + b;
};

let mul = |x, y| => x * y;

let res = add(1, 2) | mul(3);
