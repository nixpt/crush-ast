// expect-error: [runtime] Parse errors: Expected `}` but found newline at line 1, column 15, Unexpected token at ...
struct Point {
    x: Int,
    y: Int,
    label,
    metadata: Object
}

let p = Point { x: 10, y: 20 };
print(p.x);
